1.int *p[5] p是有五个元素的指针数组
  A *p[2]定义有两个对象的指针数组。
2.构造函数无任何返回类型//false
3.派生类的构造函数的初始化列表，通常包括直接基类和子对象的构造函数。
4.C++编译的单位是类
5.new为指定类型对象动态分配内存，并返回true或false。
6.char a=98；是可以的
7.一个类的友元不是该类的成员，没有this指针，也就没有默认的操作对象。
8.重载时，操作符的操作数个数不能改变
10.C++中编译时的多态性通过重载实现，运行时的多态通过虚函数实现。



3.max(int a);max(int a,int b=0);a.max(1)会产生二义性。

。
6.类的静态数据成员static int a；要初始化，int A::a=0；只能在类外定义，实际是为了消除全局变量。
7.析构函数没参数，也不能重载。
8.delete释放new的对象和对象数组，释放对象数组时delete[]p，一次释放一个对象。
9.int *p;p=new int[n];就可以new出来数组。for（int i=0;i<n;i++）a[i]=v[i];逐个赋值。或p=new int[n];copy(p,v);
10.默认的是私有派生。cout<<endl/"\n";

1.C++虚基类机制保证，公共的基类在派生类中只产生一个基类子对象。//当一个类多次间接从基类派生，其基类只能被继承一次
